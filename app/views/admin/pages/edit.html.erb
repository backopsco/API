<h1>Edit Page</h1>

<%= form_for( @page, url: admin_page_path(@page) ) do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
<% end %>

<%= link_to 'Show', admin_pages_path(@page) %> |
<%= link_to 'List Pages', admin_pages_path %>
